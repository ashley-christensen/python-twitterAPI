import { AsyncQueue } from '@sapphire/async-queue';
import type { Response } from 'undici';
import type { APIRequest } from './APIRequest';
import type { RESTManager } from './RESTManager';
export declare class RequestHandler {
    /**
     * The manager of that initiated this class
     */
    manager: RESTManager;
    /**
     * The queue for the requests
     */
    queue: AsyncQueue;
    constructor(manager: RESTManager);
    push(request: APIRequest): Promise<Record<string, unknown> | ArrayBuffer | Response>;
    execute(request: APIRequest): Promise<Record<string, unknown> | ArrayBuffer | Response>;
}
//# sourceMappingURL=RequestHandler.d.ts.map