import type { Response } from 'undici';
import type { ClientOptions } from '../client';
/**
 * Checks whether a given key is present in a given object.
 * @param targetObject The object to check the key in
 * @param key The key to check
 * @returns `true` if the key is present, otherwise `false`
 */
export declare const objectHasKey: (targetObject: Record<string, unknown>, key: string) => boolean;
export declare function mergeDefault(defaultObject: any, givenObject: any): ClientOptions;
/**
 * Parses an API response and returns the body
 * @param res The response sent by the API
 * @returns The body of the response
 */
export declare function parseResponse(res: Response): Promise<unknown | ArrayBuffer>;
//# sourceMappingURL=Utility.d.ts.map