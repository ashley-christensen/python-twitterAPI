import { Collection } from '../util';
import { BaseBook, type BaseBookOptions } from './BaseBook';
import { type User } from '../structures';
import type { Client } from '../client';
import type { UserResolvable } from '../managers';
/**
 * A class for fetching users followed by a twitter user
 */
export declare class UserFollowingsBook extends BaseBook {
    #private;
    /**
     * The Id of the user this book belongs to
     */
    userId: string;
    /**
     * @param client The logged in {@link Client} instance
     * @param options The options to initialize the book with
     */
    constructor(client: Client, options: UserFollowingsBookOptions);
    /**
     * Fetches the next page of the book if there is one.
     * @returns A {@link Collection} of {@link User} that the given user follows
     */
    fetchNextPage(): Promise<Collection<string, User>>;
    /**
     * Fetches the previous page of the book if there is one.
     * @returns A {@link Collection} of {@link User} that the given user follows
     */
    fetchPreviousPage(): Promise<Collection<string, User>>;
}
export interface UserFollowingsBookOptions extends BaseBookOptions {
    user: UserResolvable;
}
//# sourceMappingURL=UserFollowingsBook.d.ts.map