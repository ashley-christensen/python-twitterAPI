import { Collection } from '../util';
import { BaseBook, type BaseBookOptions } from './BaseBook';
import { type User } from '../structures';
import type { Client } from '../client';
import type { TweetResolvable } from '../managers';
/**
 * A class for fetching users who liked a tweet
 */
export declare class LikedByUsersBook extends BaseBook {
    #private;
    /**
     * The Id of the tweet this book belongs to
     */
    tweetId: string;
    /**
     * @param client The logged in {@link Client} instance
     * @param options The options to initialize the book with
     */
    constructor(client: Client, options: LikedByUsersBookOptions);
    /**
     * Fetches the next page of the book if there is one.
     * @returns A {@link Collection} of {@link User} who liked the given tweet
     */
    fetchNextPage(): Promise<Collection<string, User>>;
    /**
     * Fetches the previous page of the book if there is one.
     * @returns A {@link Collection} of {@link User} who liked the given tweet
     */
    fetchPreviousPage(): Promise<Collection<string, User>>;
}
export interface LikedByUsersBookOptions extends BaseBookOptions {
    tweet: TweetResolvable;
}
//# sourceMappingURL=LikedByUsersBook.d.ts.map