import { Collection } from '../util';
import { BaseBook, type BaseBookOptions } from './BaseBook';
import { type User } from '../structures';
import type { Client } from '../client';
import type { SpaceResolvable } from '../managers';
/**
 * A class for fetching users who purchased a ticket for a space
 */
export declare class SpaceTicketBuyersBook extends BaseBook {
    #private;
    /**
     * The Id of the space this book belongs to
     */
    spaceId: string;
    /**
     * @param client The logged in {@link Client} instance
     * @param options The options to initialize the book with
     */
    constructor(client: Client, options: SpaceTicketBuyersBookOptions);
    /**
     * Fetches the next page of the book if there is one.
     * @returns A {@link Collection} of {@link User} who purchased a ticket for the Space
     */
    fetchNextPage(): Promise<Collection<string, User>>;
    /**
     * Fetches the previous page of the book if there is one.
     * @returns A {@link Collection} of {@link User} who purchased a ticket for the Space
     */
    fetchPreviousPage(): Promise<Collection<string, User>>;
}
export interface SpaceTicketBuyersBookOptions extends BaseBookOptions {
    space: SpaceResolvable;
}
//# sourceMappingURL=SpaceTicketBuyersBook.d.ts.map