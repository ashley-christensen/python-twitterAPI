import { Collection } from '../util';
import { BaseBook, type BaseBookOptions } from './BaseBook';
import { type List } from '../structures';
import type { Client } from '../client';
import type { UserResolvable } from '../managers';
/**
 * A class for fetching lists followed by a user
 */
export declare class FollowedListsBook extends BaseBook {
    #private;
    /**
     * The Id of the user this book belongs to
     */
    userId: string;
    /**
     * @param client The logged in {@link Client} instance
     * @param options The options to initialize the book with
     */
    constructor(client: Client, options: FollowedListsBookOptions);
    /**
     * Fetches the next page of the book if there is one.
     * @returns A {@link Collection} of {@link List} that the given user follows
     */
    fetchNextPage(): Promise<Collection<string, List>>;
    /**
     * Fetches the previous page of the book if there is one.
     * @returns A {@link Collection} of {@link List} that the given user follows
     */
    fetchPreviousPage(): Promise<Collection<string, List>>;
}
export interface FollowedListsBookOptions extends BaseBookOptions {
    user: UserResolvable;
}
//# sourceMappingURL=FollowedListsBook.d.ts.map