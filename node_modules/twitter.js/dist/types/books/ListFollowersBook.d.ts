import { Collection } from '../util';
import { BaseBook, type BaseBookOptions } from './BaseBook';
import { type User } from '../structures';
import type { Client } from '../client';
import type { ListResolvable } from '../managers';
/**
 * A class for fetching users who are followers of a list
 */
export declare class ListFollowersBook extends BaseBook {
    #private;
    /**
     * The Id of the list this book belongs to
     */
    listId: string;
    /**
     * @param client The logged in {@link Client} instance
     * @param options The options to initialize the book with
     */
    constructor(client: Client, options: ListFollowersBookOptions);
    /**
     * Fetches the next page of the book if there is one.
     * @returns A {@link Collection} of {@link User} who follow the given list
     */
    fetchNextPage(): Promise<Collection<string, User>>;
    /**
     * Fetches the previous page of the book if there is one.
     * @returns A {@link Collection} of {@link User} who follow the given list
     */
    fetchPreviousPage(): Promise<Collection<string, User>>;
}
export interface ListFollowersBookOptions extends BaseBookOptions {
    list: ListResolvable;
}
//# sourceMappingURL=ListFollowersBook.d.ts.map